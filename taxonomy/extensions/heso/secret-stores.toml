# heso/secret-stores — first-party secret-manager host facts.

id = "heso/secret-stores"
version = 1
status = "active"
target_class = "secret_store"
primitive = "disclose"
summary = "Well-known secret manager hosts that structurally indicate secret access."
registered = "2026-06-30"

[[predicate]]
kind = "host_set"
host_suffixes = [
  ".secretsmanager.amazonaws.com", ".vault.hashicorp.cloud",
  ".secretmanager.googleapis.com", ".vault.azure.net",
]
hosts = []
